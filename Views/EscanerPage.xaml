<Page
    x:Class="DSAapp.Views.EscanerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:num="using:System.Numerics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DSAapp.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource LayerFillColorDefaultBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Label="Escanear" Command="{x:Bind ViewModel.ScanCommand}">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE8D7;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Icon="Delete" Label="Eliminar" Command="{x:Bind ViewModel.DeletePageCommand}" CommandParameter="{x:Bind ViewModel.SelectedPage, Mode=OneWay}"/>
            <CommandBar.Content>
                <TextBlock Text="{x:Bind ViewModel.TotalPagesScanned, Mode=OneWay}" VerticalAlignment="Center" Margin="12,0"/>
            </CommandBar.Content>
        </CommandBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <ListView Grid.Column="0" ItemsSource="{x:Bind ViewModel.Pages}" SelectedItem="{x:Bind ViewModel.SelectedPage, Mode=TwoWay}" Padding="10">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ScannedPageViewModel">
                        <StackPanel Margin="0,5">
                            <Image Source="{x:Bind ImageSource, Mode=OneWay}" Height="150" Stretch="Uniform"/>
                            <TextBlock Text="{x:Bind PageLabel}" HorizontalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ScrollViewer Grid.Column="1" ZoomMode="Enabled" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Image Source="{x:Bind ViewModel.SelectedPage.ImageSource, Mode=OneWay}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image.Translation>
                            <num:Vector3 Z="12" />
                        </Image.Translation>
                    </Image>
            </ScrollViewer>

            <Grid Grid.Column="2" Padding="15" Background="{ThemeResource SystemControlAcrylicElementBrush}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Texto OCR" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBox Text="{x:Bind ViewModel.SelectedPage.ExtractedText, Mode=TwoWay}" AcceptsReturn="True" TextWrapping="Wrap" Height="500" FontFamily="Consolas"/>
                    <Button Content="Copiar Texto" Click="CopyText_Click" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>