<Page
    x:Class="DSAapp.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:DSAapp.Helpers"
    xmlns:behaviors="using:DSAapp.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    Loaded="OnLoaded">

    <Grid>
        <Grid x:Name="AppTitleBar"
              Canvas.ZIndex="1"
              Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
              IsHitTestVisible="True"
              VerticalAlignment="Top">

            <StackPanel Orientation="Horizontal" Margin="28,0,0,0" VerticalAlignment="Center">
                <Image Source="/Assets/WindowIcon.ico"
                       Width="18" Height="18" 
                       Margin="0,0,12,0"/>
                <TextBlock x:Name="AppTitleBarText"
                           TextWrapping="NoWrap"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           FontWeight="SemiBold"
                           Opacity="0.8"/>
            </StackPanel>
        </Grid>

        <NavigationView
            x:Name="NavigationViewControl"
            Canvas.ZIndex="0"
            IsBackButtonVisible="Visible"
            IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
            IsSettingsVisible="True"
            ExpandedModeThresholdWidth="1008" 
            DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
            Header="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="Shell_Main" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.MainViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xe7c3;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Shell_VistaWeb" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.VistaWebViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xe774;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Registro de Oficios" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.OficiosViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Shell_CuadrículaDeDatos" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.CuadrículaDeDatosViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xe80a;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Shell_CuadrículaDeContenido" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.CuadrículaDeContenidoViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xf0e2;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Shell_DetallesDeLista" helpers:NavigationHelper.NavigateTo="DSAapp.ViewModels.DetallesDeListaViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xea37;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <Grid Margin="0,10,0,0">
                        <TextBlock Text="{Binding}" Style="{ThemeResource TitleTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </NavigationView.HeaderTemplate>

            <i:Interaction.Behaviors>
                <behaviors:NavigationViewHeaderBehavior
                    DefaultHeader="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
                    <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                        <DataTemplate>
                            <Grid Margin="0,10,0,0">
                                <TextBlock Text="{Binding}" Style="{ThemeResource TitleTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                </behaviors:NavigationViewHeaderBehavior>
            </i:Interaction.Behaviors>

            <Grid Margin="{StaticResource NavigationViewPageContentMargin}">
                <Frame x:Name="NavigationFrame">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </Grid>

        </NavigationView>
    </Grid>
</Page>